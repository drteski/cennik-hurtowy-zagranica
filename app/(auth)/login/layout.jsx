export const metadata = {
  title: "Serwis Podlasiak",
};

const LoginLayout = ({ children }) => {
  return <div className="h-screen p-10 bg-gray-900">{children}</div>;
};

export default LoginLayout;
